fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# MongoDB
motor>=3.3.0              # async MongoDB driver
pymongo>=4.6.0            # sync driver (used by motor internally)

# Auth
python-jose[cryptography]>=3.3.0   # JWT
passlib[bcrypt]>=1.7.4             # bcrypt password hashing

# Google OAuth
httpx>=0.27.0             # async HTTP client for Google token verify

# Validation
pydantic[email]>=2.0.0

# Environment
python-dotenv>=1.0.0
routers
bcrypt==4.0.1
